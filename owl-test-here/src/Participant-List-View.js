import React from 'react';
import participants from './Store.js';

//loop through each participant
//pull id, name, avatar, in session
// input participants
// output single participant
function pullingByParticipant(){
    // const id = participants.find(id => id.null);
    // const name = participants.name;
    // const avatar = participants.avatar;
    // const inSession = participants.inSession;
    console.log(participants[0].id);
}

export default pullingByParticipant